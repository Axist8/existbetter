import React from 'react';
import {style} from './index.scss';

const Routines = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

Routines.propTypes = {

};

export default Routines;
